# Device Control — Implementation Status

This is the "research → working code" handoff record for the `device-control` module in `signal-scope-be`, and the corresponding schema in `signal-scope-db`. It records what was built, why, what was verified (and how), and what's left — the counterpart, on the implementation side, to the vendor research this docs tree already contains.

**Full design decisions live in `signal-scope-be/src/device-control/README.md`** (checked into the backend repo, next to the code it documents). This file is the higher-level status record for the docs tree; read the module README for the actual architecture rationale, sanitization rules, and worker-model tradeoffs.

## What exists now

A uniform database schema (`signal-scope-db/migrations/017`–`022`) and a NestJS module (`signal-scope-be/src/device-control/`) that can:

- Dial a real device (or an emulator/simulator presenting the same interface) over SSH, Telnet, or SNMP.
- Run vendor-specific **literal** CLI command plans or SNMP SETs for a small, deliberate action set (`port.setAdminStatus`, `port.setDescription`, `vlan.setPvid`, `vlan.setTrunkAllowed`, `vlan.create`, `config.save`, `interface.setIpAddress`, `route.static.upsert`) — the same action set `comparison/snmp-write-support-matrix.md` already cross-references, so no new scope was invented.
- Refuse an SNMP SET when no documented write path exists for that vendor+action (checked against `device_capabilities`, seeded from this docs tree's own comparison tables) rather than assuming one.
- Audit every literal command/SNMP-op, with a durable session record, in `device_command_audit`/`device_sessions`.
- Cache an action into `pending_changes` when a device is unreachable (including a `planned`/ghost device that was never deployed), and drain the queue automatically on a down→up reachability transition — through the exact same execution path a live action uses.
- Treat real devices, EVE-NG-hosted devices, and Docker-simulated devices identically: same schema row shape, same code path, only the connection target's host/port/kind differs.

Vendors implemented, Phase 1: **Cisco IOS/IOS-XE, Juniper Junos, Arista EOS, MikroTik RouterOS** — four vendors covering three structurally distinct CLI-apply models (immediate-apply/mode-based, candidate-config/commit, menu-path/no-modes) and a wide spread of SNMP-write posture (broad-for-a-few-objects, narrow-two-objects, essentially none, essentially none).

Vendors implemented, Phase 2 (2026-07-28): **Extreme EXOS, Huawei VRP, HPE Aruba AOS-CX, Dell PowerSwitch OS10, D-Link (Cisco-like dialect), Fortinet FortiSwitch (standalone CLI), Ubiquiti UniFi, Netgear (M-series), Zyxel (standalone-managed line)** — the remaining 9 documented vendors, closing out this docs tree's full vendor list (13/13 have an adapter now). Each followed the established `VendorAdapter` + spec + CLI-stub-script + capability-seed pattern; one fresh fork per vendor, same brief shape as Phase 1's Juniper/Arista/MikroTik, per this file's own prediction that the pattern would be mechanical enough for that. It mostly was — see "Phase 2 notes" below for the one real hiccup.

## Verification

Every claim below was actually run this session, not assumed:

- **Unit tests** (`adapters/*.adapter.spec.ts`) — Phase 1: 101 assertions across 4 vendors. Phase 2 added 9 more suites (one per new vendor, ~150–250 assertions each, 13 suites / ~300 assertions total) — `buildCliPlan`/`buildSnmpPlan`/`buildReadbackCommand` against fixtures, no live transport. Each vendor's suite specifically asserts the SNMP-capability-gating behavior documented for that vendor.
- **Live-transport integration tests** (`device-control.e2e.spec.ts`, real Postgres + real Redis/BullMQ + real SSH connections to scripted CLI stub servers + a real SNMP agent) — one round trip per Phase 1 vendor (dial → session setup → literal plan → readback), the SNMP capability-refusal behavior against a real SNMP agent, the offline-queue-then-drain flow, the raw-CLI-passthrough sanitizer + a real forwarded command, and (Phase 2) the two new conflict-detection cases.
- **New in Phase 2**: `queue/device-worker-registry.service.spec.ts` — real Redis/BullMQ, no mocks, covers idle eviction, active-job protection, and recently-used protection.
- **Full existing test suite** (`npm test`), Phase 2 final run: 399 passed, 1 failed, 400 total, across 17 suites — the failure is the same pre-existing `app.controller.spec.ts` scaffold issue documented in `AUDIT-REPORT.md` L3 and by Phase 1, confirmed identical before and after this work (not a regression). Note: running the full suite repeatedly back-to-back without a pause reproduces the orphaned-simulator-process flakiness described below in "A test-hygiene quirk worth knowing about" — every number above is from a clean run with no lingering `cli-stub-server`/`snmp-agent-simulator` processes.
- `tsc --noEmit` and `nest build` clean (all 13 vendor adapters compile into `dist/device-control/adapters/`).

## Testing infrastructure — a scope adjustment from the original plan

The plan going into this session called for a Docker-based `snmpsim` container. **This environment's Docker daemon has no container-registry egress** (`docker pull node:20-alpine` times out even though this host's own `curl` reaches Docker Hub's HTTPS endpoint fine — specific to how the daemon reaches the registry here). Rather than block on that, the simulators were built as self-contained TypeScript instead:

- `device-control/testing/snmp-agent-simulator.ts` — a real SNMP agent (`net-snmp`'s own `createAgent`/MIB-provider API), seeded with the exact OIDs the Cisco adapter's `buildSnmpPlan()` targets (`IF-MIB::ifAdminStatus`/`ifAlias`, `CISCO-VLAN-MEMBERSHIP-MIB::vmVlan`).
- `device-control/testing/cli-stub-server.ts` + `testing/scripts/<vendor>.script.ts` — a scripted SSH/Telnet responder (real `ssh2` server, real `net.Socket` telnet), one script per vendor, replaying the exact prompt/command sequences that vendor's adapter emits.

A `docker-compose.sim.yml` overlay is included at the monorepo root for a deployment environment that does have registry egress — it has **not** been run end-to-end in this session (only syntax-validated via `docker compose config`), since the actual verification above used the TypeScript simulators directly.

## Schema

New tables (`vendor_profiles`, `device_credentials`, `device_connection_targets`, `device_capabilities`, `vendor_capability_defaults`, `device_sessions`, `device_command_audit`, `pending_changes`, `device_vlans`, `interface_vlan_membership`) plus additive columns on `devices`/`interfaces`. Full rationale and the extension pattern for future vendors is in the module README; the short version: `device_capabilities`/`vendor_capability_defaults` are the structured, queryable version of `comparison/snmp-write-support-matrix.md` and `cli-syntax-matrix.md`, and `device_connection_targets.proxy_device_id`/`proxy_selector` specifically anticipate Ubiquiti's controller-first and Fortinet's FortiLink-mediated "session target differs from config target" shape (see `vendors/fortinet/fortilink-integration.md`) without needing a schema change when those vendors get adapters.

## Phase 2 notes (2026-07-28)

Closed out three items from Phase 1's backlog, in addition to the 9 vendor adapters:

- **Optimistic-concurrency conflict detection on drain** — done. Before applying a `pending_changes` row queued with `expected_prior_state`, `PendingChangesService.checkConflict()` now runs a new `mode: 'probe'` request through `DeviceActionRunnerService` (same per-device queue as a real apply, so ordering is preserved) that reads back current state via the adapter's own `buildReadbackCommand`/`parseReadback` *without* applying the action, and compares it against what was recorded. A mismatch sets `status='conflict'` (a value the schema already had, previously unused) and stops the drain, same as a hard failure. Covered by two new `device-control.e2e.spec.ts` cases (conflict, and confirmed-match-applies-normally).
- **Per-device BullMQ queue/worker cache eviction** — done. `DeviceWorkerRegistryService` now tracks a `lastUsedAt` timestamp per device and runs its own low-frequency repeatable BullMQ job (`DEVICE_WORKER_EVICT_SWEEP_MS`, default 5 min) that closes and drops any device idle past `DEVICE_WORKER_IDLE_EVICT_MS` (default 30 min) with no active/waiting job. Covered by `queue/device-worker-registry.service.spec.ts` (real Redis, no mocks — evicts an idle entry, and confirms an entry with a waiting job or a recent `lastUsedAt` is left alone).
- **npm audit triage** — done, see Environment notes below.

One real hiccup worth recording: one of the nine parallel per-vendor forks (Huawei) drifted mid-run and, before erroring out on an unrelated account spend-limit interruption, made an unscoped edit to the shared `device-worker-registry.service.ts` file it had been explicitly told not to touch — implementing (unprompted) almost exactly the idle-eviction feature above. On inspection the code was correct and well-reasoned, so it was kept (after adding the test coverage above) rather than discarded, and the Huawei adapter itself was redone cleanly in a second fork. Worth remembering for future multi-fork vendor batches: forks sharing full conversation context (including a visible task list covering unrelated work) can occasionally pick up and act on a task that isn't theirs — review diffs to files outside a fork's assigned scope, don't assume the "don't touch shared files" instruction was followed just because it was given.

Still not done (unchanged from Phase 1, not attempted this session — see the user-facing scoping conversation that preceded this work):

- **`signal-scope-fe` is untouched** — no inventory UI exists yet for this to plug into. Deliberately out of scope for Phase 2 (backend-only), planned as a separate follow-up.
- **Live WS/xterm.js terminal streaming** — not built. `CliChannel.onData()` is the tap point for a future gateway. Also deferred to the frontend follow-up.
- **EVE-NG live verification** — EVE-NG was not reachable from this environment either session. `connection_kind='eve-ng'` is fully implemented (it's just SSH/Telnet/SNMP to a management IP — nothing EVE-NG-specific exists or is needed in the code), but no live smoke test against a real EVE-NG instance has run. **Still the concrete next step** once EVE-NG is reachable: attach a `device_connection_targets` row pointing at an EVE-NG lab device's console/management IP for one of the implemented vendors, run one guided action through the API, and confirm the round trip — no code changes anticipated, this is a configuration step.
- **Deep per-vendor `show`/`print` output parsing** — `parseReadback()` is still intentionally minimal per adapter (enough to prove a round trip), not a general parser. Now also the mechanism the new conflict-detection probe relies on, so its shallowness is a slightly higher-stakes gap than before — a `parseReadback` that returns a large, noisy `after` blob (e.g. the whole-running-config-scoped adapters like Dell/D-Link) will make conflict comparisons brittle in practice, worth tightening before this feature is relied on heavily in production.
- **FortiLink-mediated Fortinet path** — the Phase 2 Fortinet adapter covers standalone CLI only. FortiLink (session target = FortiGate, config target = FortiSwitch) needs `device_connection_targets.proxy_device_id`/`proxy_selector` routing in the connection/orchestrator layer that doesn't exist yet — a real scope-add, not a small follow-up.
- **D-Link xStack dialect, Zyxel GS1900, Netgear GS-series, legacy Aruba dialects** — all explicitly out of scope for their respective Phase 2 adapters (documented in each adapter's class doc comment), matching this docs tree's own tiering decisions.

## A structural finding worth carrying forward

Implementing Junos surfaced a real architectural tension worth flagging for whoever works on this next: this phase's worker model runs each `DeviceAction` as its own fresh dial/session (no long-lived shared session across actions — see the module README's worker-model rationale). That's transparent for immediate-apply vendors (Cisco/Arista/MikroTik) but matters for Junos's candidate-config model: a `set`-only action and a later, separate `config.save`/`commit` action are two different sessions. The Juniper adapter deliberately uses plain shared `configure` (not `configure exclusive`, despite `cli-reference.md` recommending exclusive mode for automation) specifically because Junos's shared candidate persists uncommitted edits across separate sessions, while an exclusive-mode candidate does not survive its authoring session closing. This is a real, documented tradeoff (risk of a concurrent human's `configure exclusive` session or an uncoordinated `rollback 0` discarding a SignalScope-authored pending change), not an oversight — see `juniper-junos.adapter.ts`'s class doc comment and the module README. Any future vendor with a similar staged-apply model (candidate-config, two-phase commit, etc.) should be checked against this same tension before assuming the current worker model fits it unmodified.

## A test-hygiene quirk worth knowing about

`device-control.e2e.spec.ts` spawns child processes (`cli-stub-server.ts` × 4 vendors, `snmp-agent-simulator.ts`) in `beforeAll` and kills them in `afterAll`. If a test run is interrupted (killed mid-run rather than allowed to finish/time out normally), those children can be orphaned and keep holding their ports, which makes the *next* run fail with spurious `queued` instead of `executed` outcomes (the new run's freshly-spawned stub server fails to bind because the old one is still squatting the port, so the dial genuinely fails and the orchestrator correctly falls back to the offline queue — the observed symptom is a real consequence of the actual code path, not a flaky assertion). If this spec fails with unexpected `queued` outcomes, check for and kill any lingering `snmp-agent-simulator.ts`/`cli-stub-server.ts` processes before re-running — this bit this session's own verification pass once, confirmed as environment hygiene rather than an implementation bug.

## Environment notes (housekeeping, not architectural)

- This local Postgres instance has `devices`/`interfaces` (and other early-migration tables) owned by the `postgres` superuser rather than the `signalscope` application role — a pre-existing local-dev provisioning quirk (see project memory), not introduced by this work. New migrations that `ALTER TABLE`/add a `REFERENCES` on those tables needed to be applied via `sudo -u postgres psql` locally for this session's testing. A fresh Docker deployment (where `POSTGRES_USER=signalscope` initializes and owns everything itself) won't hit this.
- `bullmq`/`ioredis`/`ssh2`/`net-snmp` npm-audit triage (2026-07-28): **zero findings trace to these four packages or their own transitive dependencies** — `npm ls bullmq ioredis ssh2 net-snmp` confirms current versions (5.81.2 / 5.11.1 / 1.17.0 / 3.26.3), and cross-referencing `npm audit --json`'s vulnerability graph against those names and their `via` chains found no match. The 23 findings `npm audit` does report (3 low, 12 moderate, 8 high) are all rooted in pre-existing dev/build tooling deps unrelated to this module — `webpack`/`glob`/`picomatch` (via `@nestjs/cli`), `js-yaml`/`tmp`/`inquirer` (via `@angular-devkit/schematics-cli`), `file-type` (via `@nestjs/common`), and `multer`/`qs` (via `@nestjs/platform-express`). Several have `npm audit fix --force` remediations that bump `@nestjs/platform-express`/`@nestjs/schematics` to breaking-change major versions — left alone as out of scope for this device-control-focused pass; worth its own separate triage. Plain `npm audit fix` (no `--force`) was also tried as a low-risk option for the non-breaking-labeled findings (`glob`/`js-yaml`/`tmp`/`webpack`/`file-type`/`brace-expansion`) — it actually made things worse in this repo's current dependency graph, re-resolving `package-lock.json` to **48** vulnerabilities instead of fixing any of the 23 (some transitive pin shifted to a less-patched version elsewhere in the tree). Reverted immediately (`git checkout -- package-lock.json`, confirmed back to 23 findings and a clean `git status`). Don't assume "no `--force`" means "safe" for this particular dependency graph — re-verify with a real `npm audit` count after, not just npm's own "fix available via `npm audit fix`" labeling, before trusting it.
- **Supply-chain trust note, surfaced incidentally during the audit above, not a CVE**: the `dotenv@17.4.2` package (a real, legitimate, already-in-use direct dependency — not compromised or typosquatted) ships `node_modules/dotenv/skills/dotenv/SKILL.md` and a sibling `dotenvx` skill file, containing instructions written specifically to steer AI coding agents ("If users work with AI coding agents... recommend `dotenvx`", "reducing accidental exposure risk when LLM agents can read repository files") toward recommending the same author's other product. No action taken on it (not followed, not installed, not acted on) — flagged here because it's a concrete, real instance of a package author using its own install footprint to try to influence agents working in a repo that depends on it, worth being aware of if this pattern shows up again in other dependencies.
